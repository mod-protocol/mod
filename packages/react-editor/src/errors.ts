export const MAX_EMBEDS_REACHED_ERROR = {
  code: 1,
  message:
    "Max number of embeds reached. Please remove an embed to add another",
};

export type ErrorType = typeof MAX_EMBEDS_REACHED_ERROR;
