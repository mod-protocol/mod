import { useEditor } from "./use-editor";
import { useTextLength } from "./use-text-length";
import { EditorContent } from "@tiptap/react";
export type { Editor } from "@tiptap/core";

export { useEditor, useTextLength, EditorContent };
